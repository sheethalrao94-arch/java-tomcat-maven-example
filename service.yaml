apiVersion: v1
kind: Service

metadata:
  name: java-tomcat-service

spec:
  type: NodePort

  selector:
    app: java-tomcat-app

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007
